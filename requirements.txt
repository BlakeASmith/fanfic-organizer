# Library / CLI runtime (pip install -r requirements-dev.txt in a checkout).
# The plugin zip vendors these except native/Calibre packages (lxml, Pillow).
beautifulsoup4>=4.12
lxml>=5.0
requests>=2.31
python-dotenv>=1.0
pyyaml>=6.0
pillow>=10.0
